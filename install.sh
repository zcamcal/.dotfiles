ln -sf ~/.dotfiles/sway ~/.config/sway
ln -sf ~/.dotfiles/waybar ~/.config/waybar
ln -sf ~/.dotfiles/tmux ~/.config/tmux

## TODO: learn more about stow and use it because ln if symlink exists will append other symlink ;(
